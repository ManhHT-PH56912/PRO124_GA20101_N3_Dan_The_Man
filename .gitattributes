# Chỉ định các loại tệp xử lý dòng xuống dòng trên các nền tảng khác nhau
# Unity text files (e.g., .meta files)
*.cs             text diff=csharp
*.shader         text
*.cginc          text
*.json           text
*.xml            text
*.yaml           text
*.yml            text
*.asmdef         text

# Unity assets
*.unity          binary
*.prefab         binary
*.fbx            binary
*.png            binary
*.jpg            binary
*.psd            binary
*.tga            binary
*.mp3            binary
*.wav            binary
*.ogg            binary
*.mp4            binary
*.mov            binary
*.anim           binary
*.controller     binary

# Các tệp cài đặt của Unity
ProjectSettings/ProjectVersion.txt text
ProjectSettings/TagManager.asset text
ProjectSettings/AudioManager.asset text
ProjectSettings/EditorBuildSettings.asset text
ProjectSettings/DynamicsManager.asset text
ProjectSettings/Physics2DSettings.asset text
ProjectSettings/PhysicsManager.asset text
ProjectSettings/TimeManager.asset text
ProjectSettings/UnityConnectSettings.asset text
ProjectSettings/QualitySettings.asset text

# Git LFS cho các file lớn (yêu cầu cài đặt Git LFS)
# Nếu bạn sử dụng Git LFS, cấu hình dưới đây sẽ giúp quản lý file lớn
*.psd filter=lfs diff=lfs merge=lfs -text
*.fbx filter=lfs diff=lfs merge=lfs -text
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.mov filter=lfs diff=lfs merge=lfs -text
*.wav filter=lfs diff=lfs merge=lfs -text

# Thiết lập mặc định
# Chỉ định cách xử lý ký tự xuống dòng cho tất cả các tệp
* text=auto

# Đảm bảo rằng các file nhị phân không bị đổi line ending
*.png -text
*.jpg -text
*.exe -text
*.dll -text
